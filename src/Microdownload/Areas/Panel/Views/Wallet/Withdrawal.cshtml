@model WalletChargeViewModel
@{
    ViewData["Title"] = "برداشت از کیف پول";
}
<form asp-controller="Wallet"
      asp-action="Withdrawal"
      asp-route-returnurl="@ViewData["ReturnUrl"]"
      method="post"
      autocomplete="off"
      class="form-horizontal">
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">@ViewData["Title"]</h3>
        </div>
        <div class="panel-body">
            <div class="alert alert-warning">
                <a href="#" class="close" data-dismiss="alert">×</a>
                <h2>توضیحات !</h2>
                <p>
                    <h1>@ViewBag.WalletInventory</h1>
                </p>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <fieldset>
                        <h2>مبلغ درخواستی را وارد کنید</h2>
                        <input asp-for="Id" />
                        <div class="form-group">
                            @Html.LabelFor(model => model.Amount, new { @class = "col-md-2 control-label" })
                            <div class="col-md-10">
                                <div class="input-group">
                                    @Html.TextBoxFor(model => model.Amount, new { @class = "form-control price", @required = "", dir = "ltr" })
                                    <div class="input-group-addon">تومان</div>
                                </div>
                            </div>
                        </div>
                    </fieldset>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="btn-group pull-right">
                        <button type="submit" class="btn btn-info"><i class="fa fa-save"> </i> ارسال درخواست  </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>